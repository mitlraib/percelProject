{
  "name": "party-arena",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix src/server\" \"npm run dev --prefix src/client\"",
    "build": "npm run build --prefix src/client",
    "start": "npm run start --prefix src/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}